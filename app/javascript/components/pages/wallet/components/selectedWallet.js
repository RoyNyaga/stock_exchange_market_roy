import React from 'react';
import styled from 'styled-components';

const SelectedWallet = ({ selectedWalletId }) => {
  return <Wrapper>
    <h1>This is selected wallet</h1>
  </Wrapper>;
}

const Wrapper = styled.div`

`

export default SelectedWallet;
